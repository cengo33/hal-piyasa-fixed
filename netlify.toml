[build]
  base = "Projeler/Market_Predict_AI"
    command = "npm run build"
      publish = "dist"

      [build.environment]
        NETLIFY_SKIP_SUBMODULES = "true"
          GIT_SUBMODULES = "false"
          
